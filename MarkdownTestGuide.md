# Markdown 测试页面使用指南

## 概述

新增的 `MarkdownTestViewController` 是一个专门用于测试 AITextView Markdown 渲染功能的页面，模仿了 `AIStreamTestViewController` 的设计风格。

## 功能特性

### 🎯 测试功能
- **基础 Markdown 测试**：测试常用 Markdown 语法
- **高级 Markdown 测试**：测试复杂 Markdown 功能
- **流式 Markdown 测试**：模拟 AI 流式输出效果
- **模式切换**：在普通 HTML 和 Markdown 模式间切换
- **清除内容**：清空编辑器内容

### 🚀 核心特性
- ✅ **流式渲染**：支持实时更新 Markdown 内容
- ✅ **语法补全**：自动处理不完整的 Markdown 语法
- ✅ **丰富功能**：支持表格、代码高亮、数学公式、emoji 等
- ✅ **模式切换**：可在普通 HTML 模式和 Markdown 模式间切换
- ✅ **进度显示**：流式渲染时显示进度条

## 使用方法

### 1. 访问测试页面
1. 运行 AITextViewUIKIT 项目
2. 在主页面点击右上角的"Markdown 测试"按钮
3. 测试页面将以模态方式弹出

### 2. 基础测试流程
1. **切换到 Markdown 模式**：点击"切换到 Markdown 模式"按钮
2. **选择测试功能**：点击相应的测试按钮
3. **观察渲染效果**：查看 Markdown 内容的渲染结果

### 3. 测试功能详解

#### 基础 Markdown 测试
测试常用的 Markdown 语法：
- 标题（H1-H6）
- 文本格式（粗体、斜体、删除线）
- 列表（有序、无序、嵌套）
- 链接和图片
- 引用块
- 代码块
- 表格
- 任务列表

#### 高级 Markdown 测试
测试复杂的 Markdown 功能：
- 数学公式（行内和块级）
- 复杂表格（对齐、多列）
- 代码高亮（多语言）
- 引用嵌套
- 列表嵌套
- 特殊字符和符号
- 表情符号
- 脚注
- 混合格式

#### 流式 Markdown 测试
模拟 AI 流式输出：
- 内容逐步显示
- 实时进度更新
- 语法补全演示
- 流式渲染效果

## 界面布局

```
┌─────────────────────────────────────┐
│  Markdown 渲染测试                  │
├─────────────────────────────────────┤
│                                     │
│  ┌─────────────────────────────────┐ │
│  │                                 │ │
│  │        AITextView 编辑器        │ │
│  │                                 │ │
│  │     (Markdown 内容渲染区域)      │ │
│  │                                 │ │
│  └─────────────────────────────────┘ │
│                                     │
│  [基础 Markdown 测试]               │
│  [高级 Markdown 测试]               │
│  [流式 Markdown 测试]               │
│  [切换到 Markdown 模式]             │
│  [清除内容]                         │
│                                     │
│  状态: 准备就绪                     │
│  ████████████████████ 100%         │
└─────────────────────────────────────┘
```

## 技术实现

### 核心组件
- **MarkdownTestViewController**：主测试控制器
- **AITextView**：Markdown 渲染编辑器
- **StreamMarkdownProcessor**：流式处理引擎
- **测试按钮组**：功能测试入口

### 流式渲染逻辑
1. 用户点击"流式 Markdown 测试"
2. 启动定时器，逐字符添加内容
3. 调用 `updateMarkdownStream()` 更新渲染
4. 显示进度条和状态信息
5. 完成后调用 `updateMarkdownStream(_, isComplete: true)`

### 模式切换逻辑
1. **切换到 Markdown 模式**：
   - 调用 `editorView.setupStreamMarkdown()`
   - 更新按钮状态和颜色
   - 显示 Markdown 初始内容

2. **切换到普通模式**：
   - 调用 `editorView.switchToNormalMode()`
   - 恢复按钮状态
   - 显示普通 HTML 内容

## 测试用例

### 基础语法测试
```markdown
# 标题测试
**粗体** *斜体* `代码`
- 列表项
[链接](URL)
```

### 高级功能测试
```markdown
$$E = mc^2$$
| 列1 | 列2 |
|-----|-----|
| 内容 | 内容 |
```

### 流式渲染测试
模拟 AI 逐步输出 Markdown 内容，观察渲染效果。

## 注意事项

1. **模式切换**：必须先切换到 Markdown 模式才能进行测试
2. **流式测试**：可以随时停止流式渲染
3. **内容清除**：会清空当前编辑器内容
4. **性能考虑**：大量内容建议使用流式渲染

## 扩展功能

可以轻松扩展更多测试功能：
- 添加更多 Markdown 语法测试
- 增加性能测试
- 添加错误处理测试
- 支持自定义测试内容

## 更新日志

- **v1.0.0**：初始版本
  - 基础 Markdown 测试
  - 高级 Markdown 测试
  - 流式渲染测试
  - 模式切换功能
  - 完整的 UI 界面
